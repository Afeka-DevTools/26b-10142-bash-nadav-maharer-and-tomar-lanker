# 26b-10142-bash-nadav-maharer-and-tomar-lanker
26b-10142-bash-nadav-maharer-and-tomar-lanker created by GitHub Classroom

<div dir="rtl">

### חברת צוות: אליס ריינס

כדי להשתמש בסקריפטים שכתבתי, יש תחילה לשכפל את הפרויקט לסביבת העבודה המקומית באמצעות הפקודה `git clone <your repository address>`. לאחר מכן, חובה לתת לסקריפטים הרשאות ריצה על ידי הפעלת הפקודה `chmod +x scripts/*.sh` מתיקיית השורש של הפרויקט.

* בחרתי לכתוב את `clean_temp_files.sh`, הוא מוחק קבצים זמניים ומיותרים בעלי הסיומת .pyc ו-.class ואת התיקייה node_modules. הוא רץ ללא קלט באמצעות הפקודה: `./scripts/clean_temp_files.sh`.
* בחרתי לכתוב את `count_file_stats.sh`, הוא קולט תיקייה ומדפיס את כמות השורות, המילים והתווים עבור כל קובץ בתיקייה. פקודת ההרצה לדוגמה: `./scripts/count_file_stats.sh "path/to/folder"`.
* בחרתי לכתוב את `delete_old_files.sh`, הוא קולט תיקייה ופרק זמן מסוג ימים. הוא מוחק בתיקייה קבצים שישנים יותר מקלט הימים שקיבלנו. פקודת ההרצה לדוגמה: `./scripts/delete_old_files.sh "path/to/folder" 7`.
* בחרתי לכתוב את `random_password.sh`, הוא יוצר סיסמה רנדומלית בעלת 10 תווים. הסיסמה מכילה אותיות גדולות, קטנות, ספרות וסימנים מיוחדים. הוא רץ ללא קלט באמצעות הפקודה: `./scripts/random_password.sh`.
* בחרתי לכתוב את `weather_check.sh`, הוא קולט עיר ומציג את מזג האוויר המעודכן שלה. פקודת ההרצה לדוגמה: `./scripts/weather_check.sh "Tel Aviv"`.

</div>